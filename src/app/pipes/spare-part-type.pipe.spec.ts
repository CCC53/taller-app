import { SparePartTypePipe } from './spare-part-type.pipe';

describe('SparePartTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SparePartTypePipe();
    expect(pipe).toBeTruthy();
  });
});
